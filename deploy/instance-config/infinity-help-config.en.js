/* Include this on Infinity instances that run in ENGLISH. Nothing dynamic —
   this instance always shows this one language. */
window.INFINITY_HELP_CONFIG = {
  baseUrl: "https://help.infinityhu.eu",
  lang: "en"
};
